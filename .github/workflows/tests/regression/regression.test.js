describe('Regression Test', () => {
  test('fitur lama: should return true', () => {
    expect(true).toBe(true);
  });

  test('fitur lama: should subtract correctly', () => {
    const result = 10 - 5;
    expect(result).toBe(5);
  });

  test('fitur lama: should multiply correctly', () => {
    const result = 3 * 4;
    expect(result).toBe(12);
  });
});
