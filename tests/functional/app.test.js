describe('Functional Test', () => {
  test('should return true when app is running', () => {
    expect(true).toBe(true);
  });

  test('should add two numbers correctly', () => {
    const result = 1 + 1;
    expect(result).toBe(2);
  });
});
